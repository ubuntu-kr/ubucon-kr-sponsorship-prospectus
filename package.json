{
  "name": "sponsorship-prospectus",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "marp --theme ./themes/ubucon.css --html --pdf --allow-local-files --pdf-outlines",
    "preview": "marp --theme ./themes/ubucon.css --html --preview --allow-local-files"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@marp-team/marp-cli": "^4.0.3"
  },
  "dependencies": {
  }
}
